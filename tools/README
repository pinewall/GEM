meta.cxx: fetch meta data from netcdf meta data and write to a xml file as template for configuration

xml_base.cxx: read a xml file and load data
    class XMLElement
    * XMLElement (const char * xml_file)        ; xml_parser
    - addElement (XMLElement * )
    - getChild (ElementName)
    - getElementText (ElementName)
    - writeToFile (const char * xml_file)       ; xml_writer

